<%= form_with(model: @contact, url: public_confirm_contact_path, method: :post) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :message %>
  <%= form.text_area :message %>

  <%= form.submit "送信" %>
<% end %>
