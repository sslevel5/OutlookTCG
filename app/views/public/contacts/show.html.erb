<h2>お問い合わせ詳細</h2>
<div>

  <div>件名</div>
  <div><%= @contact.title %></div>
  <div>内容</div>
  <div><%= @contact.message %></div>
  <div>対応状況</div>
  <div>
    <% if @contact.is_active %>
      <span class="text-success">未対応</span>
    <% else %>
      <span class="text-secondary">対応済</span>
    <% end %>
  </div>

</div>