<h2>投稿一覧</h2>
<div class="row">
  <% @cards.each do |card| %>
    <div class="text-center card-wrapper">
      <%= link_to admin_card_path(card) do %>
        <div>
          <%= image_tag url_for(card.image), height: 120 %>
        </div>
        <div>
          <%= card.title %>
        </div>
        <div>
          <%= card.price %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>